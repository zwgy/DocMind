{
  "name": "L2历史工具载荷投影验收",
  "threads": [
    {
      "name": "l2-historical-tool-result",
      "title": "L2历史工具载荷投影验收-{tag}",
      "turns": [
        {
          "name": "create-historical-tool-result",
          "query": "只使用 write_file 创建 /home/gem/user-data/outputs/l2-history-{tag}.txt，文件内容必须以 HISTORY-{tag} 开头并完整追加下列材料；完成后只回答 HISTORY-{tag}。\n{payload}",
          "expect": {
            "assistant_contains": ["HISTORY-{tag}"],
            "tools_include": ["write_file"],
            "files": [
              {
                "path": "/home/gem/user-data/outputs/l2-history-{tag}.txt",
                "contains": ["HISTORY-{tag}"]
              }
            ]
          }
        },
        {
          "name": "project-history-under-pressure",
          "query": "请记住上一轮标记 HISTORY-{tag}，不要调用工具，只回答 HISTORY-{tag} 和 L2-{tag}。以下材料只增加上下文压力，不改变任务：\n{payload}",
          "expect": {
            "assistant_contains": ["HISTORY-{tag}", "L2-{tag}"],
            "tools_exclude": ["read_file", "write_file", "edit_file"],
            "compaction": {
              "level": "L2",
              "status": "finished",
              "min_values": {
                "tool_arguments_projected": 1,
                "tokens_saved": 1
              }
            },
            "compaction_order": ["L1", "L2", "L3", "L5"]
          }
        }
      ]
    }
  ]
}

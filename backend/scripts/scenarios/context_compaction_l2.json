{
  "name": "L2历史工具结果投影验收",
  "threads": [
    {
      "name": "l2-historical-tool-result",
      "title": "L2历史工具结果投影验收-{tag}",
      "turns": [
        {
          "name": "create-historical-tool-result",
          "query": "只使用 execute 运行这一条命令：python -c \"print('L2-HISTORY-{tag}-' + 'X' * 11000)\"。完成后只回答 L2-HISTORY-{tag}。",
          "expect": {
            "assistant_contains": ["L2-HISTORY-{tag}"],
            "tools_include": ["execute"]
          }
        },
        {
          "name": "add-first-background-round",
          "query": "登记 L2-ROUND-ONE-{tag}。以下材料只增加上下文压力，不改变已有工具结果；不要调用工具，只回答 L2-ROUND-ONE-{tag}。\n{payload}",
          "expect": {
            "assistant_contains": ["L2-ROUND-ONE-{tag}"],
            "tools_exclude": ["read_file", "write_file", "edit_file"]
          }
        },
        {
          "name": "add-second-background-round",
          "query": "登记 L2-ROUND-TWO-{tag}。以下材料只增加上下文压力，不改变已有工具结果；不要调用工具，只回答 L2-ROUND-TWO-{tag}。\n{payload}",
          "expect": {
            "assistant_contains": ["L2-ROUND-TWO-{tag}"],
            "tools_exclude": ["read_file", "write_file", "edit_file"],
            "compaction": {
              "level": "L2",
              "status": "finished",
              "min_values": {
                "tool_results_projected": 1,
                "tokens_saved": 1
              }
            },
            "compaction_order": ["L1", "L2", "L3", "L5"]
          }
        },
        {
          "name": "project-history-under-pressure",
          "query": "现在只回答 L2-HISTORY-{tag} 和 L2-PROJECTED-{tag}，不要调用工具。以下材料只增加上下文压力，不改变任务：\n{payload}",
          "expect": {
            "assistant_contains": ["L2-HISTORY-{tag}", "L2-PROJECTED-{tag}"],
            "tools_exclude": ["read_file", "write_file", "edit_file"]
          }
        }
      ]
    }
  ]
}

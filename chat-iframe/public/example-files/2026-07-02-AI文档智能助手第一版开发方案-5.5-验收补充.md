### 5.5 验收补充

这个附件用于本地调试 chat-iframe 在多附件场景下的展示和上下文选择。

验收点：

1. 父页面脚本可以从第二个 `.item[attachment] a` 中识别文件名、大小、下载地址和 `sourceKey`。
2. iframe 收到多个附件后，`问文件` 弹窗可以同时展示并选择多个页面附件。
3. 当选中文件超过一个时，底部输入框只展示 `问文件：已选 N 个附件` 的摘要，不因为文件名过长或数量过多而变高。
4. 取消选择全部附件后，聊天请求不再携带文件上下文。


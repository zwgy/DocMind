# 上下文压缩连续会话实施方案

**目标：** 摘要超时、空响应、截断、超限或事实校验失败不再中断会话；历史可追溯，当前请求和工具协议保持完整。

**设计依据：** 用户已确认的“确定性收缩保证连续性，语义摘要提升记忆质量”方案。本文件取代 2026-07-31 方案中“摘要失败立即结束、不允许无摘要收缩”的旧约束。

**技术栈：** Python 3.12、LangChain/LangGraph、现有线程文件后端、Docker Compose。

## 边界

- 在现有压缩控制器和纯消息投影模块中实现，不添加领域 Skill 特判、新服务或新数据库表。
- 完整历史与发送给模型的活动视图分开。归档成功后才提交消息收缩；归档失败时保留原检查点，只投影本次请求。
- 不吞掉取消信号、不重放工具、不将主模型故障伪装为成功。
- 部署窗口必须能容纳基本系统指令及最新请求的可读入口；固定配置故障保留明确诊断。

## 实施与验收

### 1. 连续会话回归

- [x] 在 `backend/test/unit/middlewares/test_context_compaction_middleware.py` 增加同步/异步摘要异常、错误旧摘要、归档失败的续聊测试。
- [x] 在 `backend/test/unit/agents/test_context_compaction_stack.py` 验证真实 LangGraph 栈中失败后的下一轮仍可继续。
- [x] 在独立容器测试目录运行新增测试，13 条旧实现失败；已完成错误轮次归档测试及恢复后再次溢出的 2 条测试也完成先失败、再通过验证。

核心断言：

```python
assert result.model_response.result[-1].content == "继续回答"
assert estimate_model_request(sent_request).admission <= resolve_context_budget(sent_request).prompt_budget
assert current_user in sent_request.messages
```

### 2. 确定性恢复和有界摘要

- [x] `context_projection.py`：已完成错误/确认轮次可归档，近期完整轮次继续保护。
- [x] `context_compaction.py`：摘要失败后归档原消息及旧摘要，以预算选择最新请求和完整近期轮次，使用准确归档入口继续调用主模型。
- [x] 归档不可用时不推进历史水位，不删除持久消息，不声称归档成功。
- [x] 摘要采用整次 30 秒/4 次调用边界；失败回退不再依赖另一次摘要成功。
- [x] 摘要输出预算和装入请求的预算分别校验，错误旧摘要不再晋升为可信精确事实。
- [x] 相关回归 139 项通过，Ruff、格式检查通过；独立审查发现的“恢复后溢出重发相同视图”已以拒绝视图为边界修复并覆盖同步/异步回归。既有依赖警告 10 条。

### 3. 部署及真实验收

- [x] 本地提交、推送，远端快进同步，仅重启受影响 API/Worker。
- [x] 使用现有真实来文和失败会话历史验证恢复及后续追问，不构造业务记录。
- [x] 在右侧浏览器核对可见对话与结果；记录仍存在的模型效果限制。

### 真实验收证据（2026-09-08）

- 实现提交：`751d57834cb47a3b0bb175ece4be1dee1fe88d11`；远端 `/mnt/docMind-dev-v1.0` 已同步，`api-dev` 健康、`worker-dev` 运行。部署后在实际源码上再次运行相关测试：139 passed，10 条既有警告。
- 右侧浏览器沿用历史故障会话 `真实来文流程图回归-0903-151046-2bdb78`，thread ID 为 `e77573c8-3756-4dd9-88f1-8034d145a2ab`，没有创建假来文。模型为本地 `qwen3.6:35b`，上下文窗口 32768，输入预算 27648。

| Run ID | 用时 | 真实压缩恢复 | 会话结果 |
| --- | --- | --- | --- |
| `70a61c24-294d-4a0d-96f4-cd9d9b28c2d7` | 54.4 秒 | 1 次摘要超时，恢复后 27087 tokens | completed，正确确认文号及未完成待办 |
| `790f72bb-dbd6-4979-8ac6-448447694d4b` | 167.2 秒 | 3 次摘要超限，分别恢复至 27482、27436、24549 tokens | completed，后续原文追问未中断 |
| `f907bfdc-d0bb-4892-a37a-b5654995659f` | 89.3 秒 | 1 次摘要超限，恢复至 26767 tokens | completed，自然语言纠正后可继续 |
| `053a1a7a-375d-49a3-ac3c-8386efb80cc1` | 74.2 秒 | 1 次摘要超限，恢复至 27648 tokens | completed，再次纠正后可继续 |

- 已独立读取 revision 2 的恢复 JSONL 归档：37 条记录、42761 字节，保留历史消息及带 `historical_summary_unverified` 标记的旧摘要。归档不可写等故障由自动化回归验证，未在真实部署中破坏存储来制造故障。
- 连续性验收通过，原文逐字引用验收未通过：模型仍有调用旧工具、抄错 ID、把内部附件 ID 当来源 ID，以及把结构化概括当原文的问题；最后一轮仍将“72小时”改为“72h”并拼接原句。真实原文独立核对为第十六条（一），适用国铁路用客车，但不能据此把模型的逐字引用判为正确。
- 本次不为这些模型执行偏差增加来文专用压缩逻辑。摘要失败隔离不等于保证小模型记忆与引用完全准确，也不掩盖主模型服务故障或用户取消。

测试命令（Docker 内）：

```bash
uv run --no-sync pytest test/unit/middlewares/test_context_compaction_middleware.py test/unit/middlewares/test_context_projection.py test/unit/middlewares/test_token_usage_middleware.py test/unit/middlewares/test_output_continuation_middleware.py test/unit/agents/test_context_compaction_stack.py -q -p no:cacheprovider --tb=short
```

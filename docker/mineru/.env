# nvidia-smi 中分配给 MinerU 的 GPU 编号
MINERU_GPU_DEVICE_ID=0

# 24GB GPU 与其他服务共享时建议从 0.7 开始；独占时可提高到 0.85
MINERU_GPU_MEMORY_UTILIZATION=0.7

# 对外监听地址和端口；跨主机访问时通过防火墙限制来源 IP
MINERU_BIND_HOST=0.0.0.0
MINERU_PORT=30001
